// History: Jan 07 14 tcolar Creation

package gollections

import ()

type Map struct {
	// TBD only slice done yet

	/*
	   Append(k, v)
	   AppendAll ?
	   addMap(Map)
	   All -> same as map
	   Any -> same as map
	   caseInsensitive
	   Clear
	   ContainsKey
	   Clone
	   //CloneKeys ? clone only for some keys ?
	   each
	   eachWhile
	   Find
	   FindAll
	   Get
	   GetOrAdd (key, ptr, defaultVal)
	   IsEmpty
	   Join ??
	   Keys
	   Len
	   Map -> new map applying function
	   Reduce
	   Remove
	   Set
	   String
	   Vals
	*/
}
