max -> use less
min -> use less

peek
pop
push
insert
insertAll

removeVal
removeVals
removeAt
removeRange
removeFunc(|index, val| c)

set

reverse
map |index, value| -> interface{} Create a new list which is the result of calling c for every item in this list. The new list is typed based on the return type of c
reduce (Obj? init, |Obj?,V,Int->Obj?| c)

? intersection (items in both list)
? union -> append then unique ??
? unique -> proper set, no duplicates

